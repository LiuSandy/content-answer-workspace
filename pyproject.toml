[project]
name = "content-answer-workspace"
version = "1.0.0"
description = "Content collection and answer generation workspace"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.116.0",
  "httpx>=0.28.1",
  "jinja2>=3.1.6",
  "openai>=1.93.0",
  "pydantic>=2.11.7",
  "python-dotenv>=1.1.1",
  "uvicorn>=0.35.0",
]

[tool.uv]
package = false
